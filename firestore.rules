rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permite acceso a usuarios autenticados mientras se estabiliza el sistema de roles
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
