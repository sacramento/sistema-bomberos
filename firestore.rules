
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Reglas maestras para permitir operación mientras se estabiliza el sistema
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
