// This file is no longer needed and is intentionally left empty.
// The main AppLayout at /app/(app)/layout.tsx will handle the dashboard page.
export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>;
}
